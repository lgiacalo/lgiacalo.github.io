{
  "name": "todolist",
  "version": "1.0.0",
  "description": "todolist with parcel + pages.js + localStorage",
  "main": "./main.js",
  "scripts": {
    "start": "parcel ./index.html --out-dir dist",
    "prebuild-dev": "shx rm -rf dist/*",
    "build-dev": "parcel build ./index.html --out-dir dist --no-minify --public-url ./",
    "prebuild": "shx rm -rf dist/*",
    "build": "parcel build ./index.html --out-dir dist --public-url ./"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "sass": "^1.26.11",
    "shx": "^0.3.2"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "parcel-bundler": "^1.12.4"
  }
}
